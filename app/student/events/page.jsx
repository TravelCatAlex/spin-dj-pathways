'use client';

import StubView from '../../components/molecules/StubView';

/** PAGE — /student/events. Stubbed for this UI-only milestone. */
export default function StudentEventsPage() {
  return (
    <StubView tab="events" title="Events" message="Coming soon: event calendar and opportunities" />
  );
}
